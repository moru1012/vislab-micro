spring:
  application:
    name: gateway-service
  cloud:
    config:
      enabled: false
eureka:
  client:
    serviceUrl:
#      defaultZone: http://eureka:8761/eureka/
      defaultZone: http://localhost:8761/eureka/
